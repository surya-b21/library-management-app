version: '3.8'

networks:
  go:
    driver: bridge

volumes:
  goroot:
  gocache:
  pgdata:

services:
  auth_db:
    image: postgres:16-alpine
    restart: always
    shm_size: 128mb
    environment:
      POSTGRES_PASSWORD: password
      POSTGRES_DB: auth_db
    ports:
      - 5432:5432